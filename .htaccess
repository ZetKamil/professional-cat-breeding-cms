# ============================================================
# HODOWLA KOTÓW Z MAZOWIECKIEJ SZWAJCARII
# Production .htaccess — Apache 2.4 / Laravel 12
# ============================================================

<IfModule mod_rewrite.c>
    RewriteEngine On

    # --------------------------------------------------------
    # 1. HTTPS redirect (before everything else)
    # --------------------------------------------------------
    RewriteCond %{HTTPS} off
    RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [R=301,L]

    # --------------------------------------------------------
    # 2. www → non-www canonical redirect
    # --------------------------------------------------------
    RewriteCond %{HTTP_HOST} ^www\.kotyzmazowieckiejszwajcarii\.pl$ [NC]
    RewriteRule ^ https://kotyzmazowieckiejszwajcarii.pl%{REQUEST_URI} [R=301,L]

    # --------------------------------------------------------
    # 3. Handle Authorization Header (for API / Bearer tokens)
    # --------------------------------------------------------
    RewriteCond %{HTTP:Authorization} .
    RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]

    # --------------------------------------------------------
    # 4. Handle X-XSRF-Token Header (for Livewire / AJAX)
    # --------------------------------------------------------
    RewriteCond %{HTTP:x-xsrf-token} .
    RewriteRule .* - [E=HTTP_X_XSRF_TOKEN:%{HTTP:X-XSRF-Token}]

    # --------------------------------------------------------
    # 5. BLOCK: all dotfiles & hidden directories (.env, .git, etc.)
    # --------------------------------------------------------
    RewriteRule ^\. - [F,L]

    # --------------------------------------------------------
    # 6. BLOCK: sensitive root configuration & build files
    # --------------------------------------------------------
    RewriteRule ^(composer\.(json|lock)|package(-lock)?\.json|phpunit\.xml|pint\.json|rector\.php|artisan|vite\.config\.(js|ts)|README\.md)$ - [F,L]

    # --------------------------------------------------------
    # 7. BLOCK: sensitive project directories
    #    (app, bootstrap, config, database, resources,
    #     routes, storage private logs/framework, tests, vendor, image, AI)
    # --------------------------------------------------------
    RewriteRule ^app(/.*)?$ - [F,L]
    RewriteRule ^bootstrap(/.*)?$ - [F,L]
    RewriteRule ^config(/.*)?$ - [F,L]
    RewriteRule ^database(/.*)?$ - [F,L]
    RewriteRule ^resources(/.*)?$ - [F,L]
    RewriteRule ^routes(/.*)?$ - [F,L]
    RewriteRule ^storage/(app|framework|logs)(/.*)?$ - [F,L]
    RewriteRule ^tests(/.*)?$ - [F,L]
    RewriteRule ^vendor(/.*)?$ - [F,L]
    RewriteRule ^image(/.*)?$ - [F,L]
    RewriteRule ^node_modules(/.*)?$ - [F,L]
    RewriteRule ^AI(/.*)?$ - [F,L]

    # --------------------------------------------------------
    # 8. ALLOW: public/ subdirectories (assets, images, etc.)
    # --------------------------------------------------------
    RewriteCond %{REQUEST_URI} ^/public/ [NC]
    RewriteRule ^ - [L]

    # --------------------------------------------------------
    # 9. Redirect Trailing Slashes (not a folder)
    # --------------------------------------------------------
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_URI} (.+)/$
    RewriteRule ^ %1 [L,R=301]

    # --------------------------------------------------------
    # 10. Laravel front controller
    # --------------------------------------------------------
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [L]

</IfModule>

# ============================================================
# SECURITY: Disable directory listing globally
# ============================================================
Options -Indexes -MultiViews +FollowSymLinks

# ============================================================
# SECURITY: Block sensitive file types by extension
# ============================================================
<FilesMatch "\.(env|sqlite|sqlite3|log|bak|backup|sql|key|pem|cert|crt|p12|pfx|sh|bash|py|pl|cgi)$">
    <IfModule mod_authz_core.c>
        Require all denied
    </IfModule>
    <IfModule !mod_authz_core.c>
        Order allow,deny
        Deny from all
    </IfModule>
</FilesMatch>

# ============================================================
# SECURITY: Block sensitive config/build files in root
# ============================================================
<FilesMatch "^(composer\.(json|lock)|package(-lock)?\.json|phpunit\.xml|pint\.json|rector\.php|artisan|vite\.config\.(js|ts)|README\.md|\.env.*)$">
    <IfModule mod_authz_core.c>
        Require all denied
    </IfModule>
    <IfModule !mod_authz_core.c>
        Order allow,deny
        Deny from all
    </IfModule>
</FilesMatch>

# ============================================================
# SECURITY: Block script execution in upload directories
# ============================================================
<FilesMatch "\.(php|phtml|phar|sh|bash|py|pl|cgi)$">
    <If "%{REQUEST_URI} =~ m#^/(public/)?storage/#">
        <IfModule mod_authz_core.c>
            Require all denied
        </IfModule>
        <IfModule !mod_authz_core.c>
            Order allow,deny
            Deny from all
        </IfModule>
    </If>
</FilesMatch>

# ============================================================
# PERFORMANCE: Cache static assets aggressively
# ============================================================
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType text/css "access plus 1 year"
    ExpiresByType application/javascript "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 month"
    ExpiresByType image/png "access plus 1 month"
    ExpiresByType image/webp "access plus 1 month"
    ExpiresByType image/svg+xml "access plus 1 month"
    ExpiresByType image/x-icon "access plus 1 year"
    ExpiresByType font/woff2 "access plus 1 year"
</IfModule>

# ============================================================
# SECURITY HEADERS (modern, robust)
# ============================================================
<IfModule mod_headers.c>
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-Frame-Options "SAMEORIGIN"
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
    Header always set Permissions-Policy "camera=(), microphone=(), geolocation=()"
</IfModule>
